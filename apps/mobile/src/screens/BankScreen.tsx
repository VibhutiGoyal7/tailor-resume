import { ScreenContainer } from '../components/ScreenContainer';
import { ComingSoon } from '../components/ComingSoon';

// Experience Bank — items + bullet accept/edit/reject + Resume Basics. M8 slice 3.
export function BankScreen() {
  return (
    <ScreenContainer>
      <ComingSoon title="Experience Bank" note="Your reusable experience lives here — Milestone 8, slice 3." />
    </ScreenContainer>
  );
}
